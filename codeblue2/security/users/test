#user's password
PASSWORD
	xxx

#directories that the user can read, one per line
#default is none, which means the user cannot read anything, same for write and exec
READ_PERMISSION
	/test
	/angle
	/

#directories that the user can write or modify
WRITE_PERMISSION
	/angle

#directories that the user can process the data
EXEC_PERMISSION

#list of IP addresses from which the user is allowed to login. 
#this field must exist, otherwise the user will not be able to login
ACL
	0.0.0.0/0

#the user's quota, which is the size limit of all the directories he/she has the write permission
QUOTA
	1000000
