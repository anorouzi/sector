C++ = g++

DEBUG = 1
STATIC = 0

CCFLAGS = -I./ -I../util -I../udt -I../gmp -I../routing -I../client -I../server
LDFLAGS = -lstdc++ -lpthread -L. -L../lib -lutil

ifeq ($(DEBUG), 1)
   CCFLAGS += -Wall -g
endif

ifeq ($(STATIC), 1)
   LDFLAGS += -static
endif
